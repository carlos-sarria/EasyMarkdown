{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for EasyMarkdown. Grants the main window permission to open the OS file-picker dialog and read files from the local filesystem.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    {
      "identifier": "fs:allow-read-text-file",
      "allow": [{"path": "**"}]
    }
  ]
}
